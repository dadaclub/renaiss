export function Eyebrow({ children }: { children: React.ReactNode }) {
  return (
    <div className="text-[10px] tracking-[0.22em] text-amber font-extrabold uppercase mb-2">
      {children}
    </div>
  );
}
